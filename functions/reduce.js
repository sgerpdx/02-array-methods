const arrayReduce = (array, callback) => {
  let arrNew = [];
  for (let index = 0; index < array.length; index++) {
    // console.log(arrNew);
  }
  return arrNew;
};

module.exports = arrayReduce;
